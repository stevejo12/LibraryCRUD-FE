import React from "react";

function FrontPage() {
  return (
    <div className="frontPage">
      {/* Header */}
      {/* Header => Logo + Link(Login, Contact Us, Location)*/}
      {/* Dropdown Book List (Different Categories + eBooks) + Library Services (Library Cards, Borrowing Material) */}
      {/* Body */}
      {/* Search Books or eBooks */}
      {/* Books Tab => Adult, Teen, Children (new ebook, new print fiction/non-fiction) */}
      {/* Footer */}
    </div>
  );
}

export default FrontPage;
